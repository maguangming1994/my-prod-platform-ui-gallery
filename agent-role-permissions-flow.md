# 代理核心操作与权限流程

## 权限边界

- 代理没有商品创建、商品编辑、商品发布或商品审核权限。
- 普通商品、课程、虚拟商品和预约服务均由 OPC 负责发布与履约。
- 代理库存仅包含付费星卡；代理只能采购平台或上级指定的星卡方案。
- 代理可在自己的代理层级与经营区域内审核下级代理申请。
- 代理可提交本区域销售申请，并审核权限范围内的本区域销售申请。
- 跨区域销售转移、平台主管销售归属调整和平台级最终裁决不属于代理权限。

## 移动端主流程

### 1. 代理经营中心

入口：`账户与资产 → 经营身份 → 代理 → 代理经营中心`

页面只展示三组核心任务：

1. 星卡采购与库存
2. 下级代理审核
3. 本区域销售管理

页面固定显示当前代理等级、经营区域和权限范围，并展示“不支持发布商品”的边界说明。

### 2. 星卡采购

`代理经营中心 → 星卡采购 → 选择指定方案 → 查看批次与采购价 → 确认数量 → 服务端报价 → 支付/结果查询 → 入库`

- 只允许采购付费星卡，不出现普通商品 SKU。
- 未激活代理的首批进货应使用平台指定方案；普通补货使用已生效代理权限。
- 价格、折扣、库存、席位与最终入库结果必须来自服务端。

### 3. 下级代理审核

`代理经营中心 → 下级代理待审核 → 申请详情 → 区域冲突/资质/首批方案检查 → 通过或驳回 → 审核结果`

- 只允许审核本代理层级的直接下级和授权区域。
- 越级、跨区域、席位冲突或资料不全时禁止通过。
- 通过仅代表代理侧审核完成；如规则要求平台复核，应进入“待平台复核”。

### 4. 本区域销售提交与审核

`代理经营中心 → 本区域销售 → 提交销售申请 / 待我审核 → 填写归属与区域 → 校验手机号和身份 → 提交 → 审核详情 → 通过或驳回`

- 提交人必须选择本代理可管理的区域与团队归属。
- 代理仅审核本区域销售；跨区域转移需要平台主管专项权限。
- 同一申请重复提交、手机号已绑定、区域越权和归属冲突均需明确错误态。

## 关键状态

| 场景 | 状态 |
|---|---|
| 星卡采购 | 待报价、待支付、支付待确认、已入库、库存不足、席位冲突、已退款 |
| 下级代理 | 待审核、资料待补、待平台复核、已通过、已驳回、已失效 |
| 区域销售 | 草稿、待审核、已通过、已驳回、手机号已绑定、区域越权、归属冲突 |

## UI 验收

- 任何代理页面不得出现“发布商品”“商品上架”“商品审核”。
- 采购列表不得出现护肤品、箱包等普通实物，只能出现星卡方案与批次。
- 审核页必须展示申请人、申请区域、归属、资质/冲突校验和审核意见。
- 所有通过、驳回、支付和入库操作均须展示服务端状态，不允许前端伪造成功。
