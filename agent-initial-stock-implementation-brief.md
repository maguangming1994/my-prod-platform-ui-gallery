# 第 23 套画板：代理首次星卡进货实施演进 brief

## 一句话定义

第 23 套 PNG 保留为 V093 的待支付视觉基线；当前 V094 代码已继续完成可信付款证据、唯一席位竞争、代理激活与星卡库存原子入账骨架。画板和说明必须同时标明版本边界，不能把尚未接入的真实渠道预下单、验签、主动查单或原路退款表现为已经上线。

## 用户与目标

- 用户：申请状态为 `PENDING_INITIAL_STOCK` 的代理申请人。
- 用户目标：确认平台指定的首批数量和价格，创建并恢复本人唯一订单，清楚看到支付、激活或退款待处理状态。
- 设计目标：消除“未激活不能进货、未进货不能激活”的死结，并保持资金、席位和库存只由服务端可信事实推进。

## V094 当前已实现

1. 只允许申请本人读取首次进货方案和当前订单。
2. 数量来自申请的 `requiredInitialStockQuantity`；单价、代理类型折扣、币种和支付超时来自当前有效服务端规则。
3. 服务端计算总价，客户端不能修改数量、价格、折扣或币种。
4. 创建随机业务订单号；同一申请只有一个开放订单，幂等重放返回原订单。
5. 页面恢复 `AWAITING_PAYMENT`、`PAID`、`ACTIVATED`、`REFUND_REQUIRED`、`REFUNDED` 和 `EXPIRED` 等订单状态。
6. 受信内部入账边界保存不可变支付证据，并校验业务订单号、金额、币种、渠道、第三方事件/交易号和支付时间。
7. 可信证据到达后，在同一事务中竞争唯一席位、激活代理、写入首批付费星卡库存并关联结算账户。
8. 席位竞争失败或迟到付款进入 `REFUND_REQUIRED`，不会错误激活身份或增加库存。
9. 原有允许客户端提交任意订单号和付款金额的激活入口已关闭。

## 尚未实现

- 微信/支付宝真实预下单、商户账号匹配、签名验签和主动查单。
- 对公转账收款信息、银行流水核验与财务复核后台。
- `REFUND_REQUIRED` 的原支付渠道退款执行、失败重试和退款进度查询。
- 生产商户、证书和真实支付渠道联调及 UAT。

## 页面与状态

| 页面 | 核心内容 | 主操作 |
|---|---|---|
| 待首次进货申请 | 代理类型、区域、所需数量、资格截止时间 | 查看唯一首批方案 |
| 唯一首批方案 | 锁定数量、单价、折扣、总额、支付截止时间 | 创建或恢复首次进货订单 |
| 首批进货订单 | 随机订单号、金额、截止时间和服务端状态 | 刷新订单状态；真实支付入口仍不可展示为已接通 |
| 激活结果 | `ACTIVATED` 后展示代理身份与实际入库数量 | 进入代理工作台 |
| 退款待处理 | `REFUND_REQUIRED` 及原因 | 查看说明并等待原渠道退款能力接入 |

## 画板版本边界

- `boards/23-agent-initial-stock-implemented.png` 是 V093 基线，只展示 `AWAITING_PAYMENT` 与支付未接入状态，不代表 V094 全部状态已经重绘。
- V094 代码可以展示可信激活后的真实状态，但在真实渠道未接通前不得提供伪支付按钮、二维码或客户端“确认付款”。
- 任何“支付成功”“激活成功”“库存已入账”必须来自服务端最终状态；任何“退款成功”必须来自真实退款结果。
