# 第 23 套画板：代理首次星卡进货实施演进提示词

## 当前 PNG 的归档说明

现有 `boards/23-agent-initial-stock-implemented.png` 按 V093 基线生成，重点呈现唯一方案、随机订单号、`AWAITING_PAYMENT` 和支付未接入边界。它不应再被描述为 V094 的完整最终界面。

## 下一版重绘提示词

生成一张 16:9 横向中文高保真移动端 UI 画板，并排展示 4 台完整手机，标题“代理首次星卡进货 · V094 实施态”。延续星链移动端深海军蓝 `#0B1F33`、品牌青绿 `#0F766E`、冷灰背景和白色卡片体系。

1. “唯一首批方案”：平台锁定数量 3,000 张、单价 ¥1.00/张、代理折扣 0.82、应付 ¥2,460.00 和支付截止时间；强调数量、价格和币种由服务端决定，不可编辑；按钮“创建或恢复订单”。
2. “订单处理中”：随机业务订单号、`AWAITING_PAYMENT / 待支付` 或 `PAID / 已收到可信证据`，展示金额和截止时间；仅提供“刷新订单状态”，不出现微信、支付宝、二维码或客户端确认付款。
3. “已激活”：`ACTIVATED / 已激活`，展示实际获得代理区域席位、首批入库 3,000 张和进入代理工作台按钮；注明状态来自服务端原子处理结果。
4. “退款待处理”：`REFUND_REQUIRED / 需退款`，明确原因可为席位竞争失败或迟到付款，提示真实原渠道退款能力尚待接入；禁止显示“退款成功”。

状态补充区域可列出 `REFUNDED / 已退款` 与 `EXPIRED / 已过期`，但仅作为状态字典，不得制造真实成功记录。必须是可开发的真实产品界面：9:41 iOS 状态栏、8pt 间距、16–20px 圆角、细边框、克制阴影、清晰中文。禁止伪造真实预下单、签名验签、主动查单和退款结果；禁止玻璃拟态、霓虹、过度渐变、乱码和截断文案。
