# 系统流程与实施矩阵

- 更新日期：2026-08-10
- 覆盖基线：22 套画板、83 个界面
- 状态口径：`已实现` = 已有真实前后端链路；`部分实现` = 主体存在但仍缺步骤或异常分支；`仅设计` = 不可作为上线能力；`待确认` = 规则未定，禁止直接开发写流程。

## 1. 当前新增落地

| 流程 | 前端 | 后端 | 当前状态 |
| --- | --- | --- | --- |
| 多经营身份二级列表到三级详情 | `pages/roles/index` → `pages/roles/identity-detail` → 身份工作台 | 继续使用 OPC、代理、销售工作台权限查询 | 部分实现：详情已接真实状态；审核中/可申请身份仍待统一身份聚合服务 |
| OPC 保证金账户 | `pages/roles/opc-deposit` | `GET /api/v1/workbenches/opc/deposit` | 已实现只读：应缴、已缴、待补、类目最高标准、宽限期、最近流水 |
| OPC 在线缴纳 | 页面明确显示未开放原因 | 无支付会话和受信回调服务 | 仅设计：禁止使用客户端跳转结果激活 OPC |
| OPC 退出经营与退保 | 页面展示前置清算条件和待确认项 | 无退出资格快照、退保申请及原路退款状态机 | 待确认：确认前不可提交 |
| 经营身份与保证金真实实施态 | 第 22 套画板集中展示多身份、身份详情、GRACE 宽限与退保限制 | 以现有查询接口和服务端状态为准 | 已对齐：不伪造支付成功、退保提交或退款结果 |
| 代理首次进货方案与订单 | `pages/roles/manage` 的待首次进货申请卡片 | `GET .../initial-stocking-plan`、`POST .../initial-stocking-orders` | 已实现第一阶段：服务端锁定唯一数量/价格并创建随机订单号；可信支付、席位激活和退款仍未接入 |

## 2. 端到端主流程

### 普通用户消费

登录/注册 → 首页/商城/发现 → 商品详情 → 结算报价 → 创建随机订单号 → 可信支付回调 → 订单履约 → 售后/退款 → 权益或库存反转。

- 已实现：登录、目录、四类商品基础、订单、支付状态、履约、售后、资产查询。
- 缺失：真实扫呗/微信渠道全异常联调；录播课购买当前按已确认决策保持禁用；现金储值钱包尚无独立账本和渠道。
- 异常必测：支付未知/迟到、重复回调、库存冲突、退款处理中/失败、权益撤销失败、网络重试幂等。

### OPC 入驻与经营

实名 → 主体资料 → 选择类目与资质 → 平台审核 → 最高保证金标准/专属覆盖标准 → 人民币缴纳 → 可信回调入账 → OPC/类目生效 → 商品发布 → 订单履约/售后 → 月度经营结算。

- 已实现：OPC 申请与类目审核、保证金规则/账户/流水/重算、商品和订单履约、收益查询、保证金只读页。
- 缺失：资质版本/到期完整体验、保证金支付会话和回调、缴纳后原子激活、月度经营结算读模型。
- 限制：`GRACE` 期间可经营但不可提现；逾期、扣减或追偿时发布/销售/提现受限；补足后若要求恢复审核，不得由前端自行恢复。

### 代理准入与付费星卡库存

权限边界：代理不创建、编辑或发布商品；代理移动端经营能力限定为付费星卡采购与库存、授权层级内的下级代理审核、本区域销售提交与审核。跨区域销售转移与平台主管销售归属调整由平台主管专项权限处理。

申请代理类型/区域 → 后台资质与唯一席位审核 → 服务端指定唯一首批星卡方案 → 72 小时内人民币支付 → 可信回调 → 原子竞争席位 → 成功激活并入付费星卡库存；竞争失败则发起全额原路退款。

- 已实现：申请、审核基础、区域/校园范围、星卡库存账户、激活后补货报价/订单、服务端原子席位竞争函数；待首次进货申请人现可读取服务端指定的唯一方案并创建绑定申请的首批订单，客户端不能修改数量和价格。
- 关键断点：首次进货订单尚无可信支付会话/回调、支付后席位结果查询和原路退款执行闭环，因此当前订单停留在 `AWAITING_PAYMENT`，不会提前生成代理身份或库存。
- 业务边界：代理库存只有付费星卡，03/04 号画板里的耳机、面霜、箱包等普通实物是旧稿错误。

### 销售、团队与财税

销售申请/归属 → 审核 → 推广绑定 → 客户与团队业绩 → 佣金分配/预警 → 月度经营结算与预计税费 → 季度法定申报/回执。

- 已实现：销售申请、等级、归属、推广码、客户/团队、佣金、预警、季度税务批次基础。
- 缺失：统一身份切换详情、月度经营结算/预计税费读模型、季度凭证与回执的前端闭环。
- 口径：月度经营结算不等于纳税申报；季度法定申报不能被月结页面替代。

## 3. 全局异常与安全步骤

| 场景 | 必需处理 | 当前覆盖 |
| --- | --- | --- |
| 未登录/无身份/无权限 | 服务端身份、权限和数据范围校验；前端只做引导 | 已实现基础校验，需全角色 UAT |
| 网络超时/重复提交 | 所有写操作复用幂等键；查询最终服务端状态 | 主交易已覆盖，新增流程必须沿用 |
| 支付成功但页面失败 | 只由可信回调或主动查单推进状态 | 主支付已有底座；保证金/代理首批未接入 |
| 钱、星卡、库存、权益反转 | 不覆盖历史事实，写反转流水并可对账 | 已有领域底座，真实渠道仍需验收 |
| 编号隐私 | 对外编号随机、唯一、不可推测；UUID 只作内部键 | 订单/支付已加固；提现、税务、任务、合并等仍有时间型编号待整改 |
| 全局页面状态 | 加载、空态、失败、待确认、无权限均有明确下一步 | 部分页面覆盖，需组件化与逐页验收 |

## 4. 不得伪造的未完成服务

1. 现金储值钱包：不能复用收益账户；需要持牌支付机构承载、独立影子账本、不可提现本金、限额、退款、对账与功能门禁。
2. OPC 保证金支付/退保：不能用本地按钮或客户端支付结果修改保证金、类目或 OPC 状态。
3. 代理首批进货激活：不能让客户端提交任意付款凭证或自行宣布抢占席位成功。
4. 月度经营结算：只能称经营结算和预计税费，不能标记为已纳税或已申报。

## 5. 累计人工确认项

- `CONF-001`：OPC 退保风险观察期是否必需、时长及起算点。
- `CONF-002`：审核、清算、原路退款时效，以及原渠道失败、未知、部分扣减和人工处置方案。
- `CONF-003`：可退金额公式；类目退出、商品下架、在途订单/预约/售后/退款、冻结款、欠款、追偿、处罚、发票的锁定边界。
- `CONF-004`：退保允许撤销阶段及与新订单、退款、提现、补缴的并发规则。
- `CONF-005`：保证金付款主体和协议文本。渠道已确认一期同时支持微信、支付宝和对公转账，不再列入待选项。

已移出待确认：19–21 号目标画板保留 `¥5,000`，第 22 套真实实施演示保留 `¥30,000`；不再要求统一。稳定编号不会因后续确认而重排。

## 6. 后续实施顺序

1. 完成代理首次进货第二阶段：可信支付会话/回调、72 小时资格、席位结果、库存入账和竞争失败退款；第一阶段服务端唯一方案与订单入口已完成。
2. 建设保证金微信/支付宝支付会话、可信回调、对公流水复核、`PAY_IN` 流水与 OPC/类目原子生效。
3. 在 `CONF-001` 至 `CONF-005` 明确后建设退保资格快照、清算、原路退款重试和人工处置状态机。
4. 继续统一经营身份聚合/list/detail 服务与生命周期绑定。
5. 在合规和渠道确认后建设独立现金钱包；随后补月度经营结算与季度报税前端闭环。
6. 全量整改剩余可推测业务编号，补全局状态组件、权限/竞态/幂等测试与多角色 UAT。
